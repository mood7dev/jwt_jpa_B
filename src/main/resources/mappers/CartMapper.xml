<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.green.gallery_jwt_jpa.cart.CartMapper">

    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO carts (member_id, item_id)
        VALUES (#{memberId}, #{itemId})
    </insert>

    <select id="countByMemberIdAndItemId" parameterType="map" resultType="int">
        SELECT COUNT(*) FROM carts WHERE member_id = #{memberId} AND item_id = #{itemId}
    </select>

    <select id="findAllWithItemByMemberId">
        SELECT c.id, c.item_id, i.name, i.price, i.img_path AS imgPath, i.discount_per AS discountPer
        FROM carts c
        JOIN items i ON i.id = c.item_id
        WHERE c.member_id = #{memberId}
    </select>

    <delete id="deleteByCartIdAndMemberId">
        DELETE FROM carts
        WHERE id = #{cartId}
        AND member_id = #{memberId}
    </delete>

    <delete id="deleteAllCart">
        DELETE FROM carts
        WHERE member_id = #{memberId}
    </delete>
</mapper>
<!--
이 경우 id는 파라미터로 넘기지 않아도 되고, 삽입 후 MyBatis가 자동으로 생성된 키를 객체에 세팅
-->