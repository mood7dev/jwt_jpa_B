<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.green.gallery_jwt_jpa.account.AccountMapper">

    <insert id="save" parameterType="com.green.gallery_jwt_jpa.account.model.AccountJoinReq">
    INSERT INTO members (name, login_id, login_pw)
    VALUES(#{name},#{loginId},#{loginPw})
    </insert>

    <select id="findByLoginId">
    SELECT id, login_pw
    FROM members
    WHERE login_id = #{loginId}
    </select>
</mapper>
