constants:
  file:
    directory: D:\student\backend\upload

  jwt:
    issuer: green502@green.com #발행자 이름
    bearer-format: JWT
    claim-key: signedUser
    secret-key: DaeguGreenArtAcademyClass502MsaClassFightingkkkkkk #깃허브에 올라가면 안됨 지금은 연습용
    access-token-cookie-name: access-token
    access-token-cookie-path: / #모든 요청마다 쿠키값 넘어옴
    access-token-cookie-validity-seconds: 900 #15분 (s) 60*15
    access-token-validity-milliseconds: 900_000 #15분 (ms) 60*15*1000

    refresh-token-cookie-name: refresh-token
    refresh-token-cookie-path: /api/v1/account/reissue #해당 요청일 때만 쿠키값 넘어옴
    refresh-token-cookie-validity-seconds: 1_296_000 #15일 (s) 60 * 60 * 24 * 15
    refresh-token-validity-milliseconds: 1_296_000_000 #15일 (ms) 60 * 60 * 24 * 15 * 1000

spring:
  datasource:
    url: jdbc:log4jdbc:mariadb://localhost/gallery
    username: root
    password: green502
    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy

mybatis:
  configuration:
    map-underscore-to-camel-case: true
  mapper-locations: mappers/**/*.xml
